import javax.swing.*;
import java.awt.*;

public class TourTurkey extends JFrame {
    public TourTurkey()  {
        setSize(600,400);
        setTitle("TemirAir");
        setDefaultCloseOperation(2);
        setLayout(null);
    }
}
